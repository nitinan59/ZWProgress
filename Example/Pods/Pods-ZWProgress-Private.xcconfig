#include "Pods-ZWProgress.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/ZWProgress" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/ZWProgress"
OTHER_LDFLAGS = -ObjC ${PODS_ZWPROGRESS_OTHER_LDFLAGS}
PODS_ROOT = ${SRCROOT}